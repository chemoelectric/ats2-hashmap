prefix=@prefix@
exec_prefix=@exec_prefix@
libdir=@libdir@
includedir=@includedir@
host=@host@
package=@PACKAGE@
version=@VERSION@
pkgincludedir="${includedir}/${package}"

# It is recommended you override this with
# --define-variable=PATSHOME="${PATSHOME}"
PATSHOME="@PKGCONFIG_PATSHOME@"

CPPFLAGS="-I${pkgincludedir} -I${PATSHOME} -I${PATSHOME}/ccomp/runtime"

# Options for patsopt.
PATSOPTFLAGS="-IATS ${pkgincludedir}"

# Options for patscc.
PATSCCFLAGS="-IIATS ${pkgincludedir}"

Name: ${package}
Description: Ideal Hash Maps for ATS2
Version: ${version}
Requires.private: @REQUIRES_PRIVATE_PKGS@
Libs: -L${libdir} -lats2-hashmap
Cflags: ${CPPFLAGS}
